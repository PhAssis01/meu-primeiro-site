# Cabeceras de Cloudflare (Workers static assets / Pages).
# Los archivos de /assets/ llevan un hash en el nombre (los genera Vite):
# si cambian, cambia el nombre, así que se pueden guardar en caché un año.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
